// Zone images are irrelevant to what the tests exercise, and node cannot import a jpg. The vitest
// config points every image specifier here so the editor's load resolves.
export default 'stub.jpg'
